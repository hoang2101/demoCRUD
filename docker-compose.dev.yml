services:
  app:
    build:
      context: .
    container_name: symfony_app
    ports:
      - "8000:8000"  # Port của ứng dụng Symfony
    volumes:
      - .:/app
    networks:
      - symfony_network
    environment:
        MYSQL_HOST: db
        APP_ENV: dev