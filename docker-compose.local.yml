services:
  app:
    build:
      context: .
    container_name: symfony_app
    ports:
      - "8000:8000"  # Port của ứng dụng Symfony
    volumes:
      - .:/app
    environment:
      - SYMFONY_ENV=dev
    networks:
      - symfony_network